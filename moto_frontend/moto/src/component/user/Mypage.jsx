import { useEffect, useState } from "react";
import UserInfoUpdate from "../common/UserInfoUpdate";
import { Route } from "react-router-dom";

/*
export default function MyPage(){

    const [menuList, setMenuList] = useState([
        {url : '/user/info', text : "내 정보"},
        {url : 'user/EditForm', text : "정보 변경"}
    ]);

    const {loginMember,kakaoMember} = useUserStore();

    const navigate = useNavigate();

    useEffect(function(){
        if(loginMember,kakaoMember.member)
    })
    
}

return(
 <div className="mypage-wrap">
            <div className="mypage-side">
                <section className="section account-box">
                    <div className="account-info">
                        <span className="material-icons">settings</span>
                        <span>MYPAGE</span>
                    </div>
                </section>
                <section className="section">
                     <LeftMenu menuList={menuList} />
                </section>
            </div>
    <div className="mypage-content">
        <Routes>
            <Route path='info' element={<UserInfo />} />
            <Route path="pwChg" element={<UserEdutForm />} />
            <Route parh="List" element={<PotofolioList />} />
        </Routes>
    </div>
)
    */