<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.iei.user.model.dao.UpdateDao">

    <!-- 사용자 정보 수정 -->
    <update id="updateUserInfo" parameterType="kr.or.iei.user.model.dto.UserUpdateDto">
        UPDATE tbl_user
        SET 
            user_nickname = #{userNickname},
            user_email = #{userEmail},
            user_profile_img = #{userProfileImg}
        WHERE user_id = #{userId}
    </update>

    <!-- 사용자 비밀번호 수정 -->
    <update id="updateUserPassword" parameterType="kr.or.iei.user.model.dto.UserPasswordUpdateDto">
        UPDATE tbl_user
        SET user_password = #{userPassword}
        WHERE user_id = #{userId}
    </update>

</mapper>
